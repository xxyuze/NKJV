<!-- 此文件是由模板文件 ".dtpl/component/$rawModuleName.wxml.dtpl" 生成的，你可以自行修改模板

  注意：
  1. 如果没有语法高亮，且你使用的是 vscode 编辑器的话，建义在 vscode 插件库中安装 minapp-vscode 插件 -->
<span class="word {{ verse+''+index == select?'word-hover':''}}"
 wx:for="{{cutTexts}}" wx:key="index" bind:tap="bindWordTap" 
 data-index="{{index}}" data-verse="{{verse}}">
  {{item+' '}}
</span>